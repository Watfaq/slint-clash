// SPDX-License-Identifier: AGPL-3.0-or-later
// Profile page: config profile selection and management

import { Typography, ElevatedCard, FilledButton, OutlineButton, TextButton, TextField,
         MaterialText, MaterialPalette, HorizontalDivider, Icon, ScrollView } from "@material";
import { MainAdapter } from "../components/main_adapter.slint";

export { MainAdapter as ProfileViewAdapter }

component ProfileActions {
    callback add-subscription();

    HorizontalLayout {
        spacing: 8px;
        OutlineButton {
            height: 40px;
            text: "Add Subscription";
            enabled: !MainAdapter.core-busy;
            horizontal-stretch: 0;
            clicked => {
                root.add-subscription();
            }
        }
        OutlineButton {
            height: 40px;
            text: "Reload Config";
            enabled: !MainAdapter.core-busy;
            horizontal-stretch: 0;
            clicked => {
                MainAdapter.reload-config();
            }
        }
    }
}

export component ProfileView {
    width: 100%;
    height: 100%;

    ScrollView {
        VerticalLayout {
            padding: 16px;
            spacing: 16px;
            width: 100%;

            if root.width >= 560px: HorizontalLayout {
                height: 56px;
                spacing: 12px;
                width: 100%;
                VerticalLayout {
                    spacing: 4px;
                    alignment: center;
                    MaterialText {
                        text: "Profiles";
                        style: Typography.headline_small;
                    }
                    MaterialText {
                        text: "Manage local configurations and remote subscriptions.";
                        style: Typography.body_medium;
                        color: MaterialPalette.on_surface_variant;
                    }
                }
                Rectangle { horizontal-stretch: 1; }
                ProfileActions {
                    add-subscription => {
                        MainAdapter.show-import-ui = true;
                    }
                }
            }

            if root.width < 560px: VerticalLayout {
                spacing: 8px;
                MaterialText {
                    text: "Profiles";
                    style: Typography.headline_small;
                }
                MaterialText {
                    text: "Manage local configurations and remote subscriptions.";
                    style: Typography.body_medium;
                    color: MaterialPalette.on_surface_variant;
                }
                ProfileActions {
                    add-subscription => {
                        MainAdapter.show-import-ui = true;
                    }
                }
            }

            if MainAdapter.show-import-ui: ElevatedCard {
                width: 100%;
                VerticalLayout {
                    padding: 20px;
                    spacing: 12px;

                    MaterialText {
                        text: "Import Subscription";
                        style: Typography.title_large;
                    }
                    MaterialText {
                        text: "Add a remote YAML profile. Existing subscriptions can be updated later.";
                        style: Typography.body_small;
                        color: MaterialPalette.on_surface_variant;
                        wrap: word-wrap;
                    }

                    TextField {
                        label: "Profile name";
                        text <=> MainAdapter.import-name;
                    }

                    TextField {
                        label: "Subscription URL";
                        text <=> MainAdapter.import-url;
                    }

                    HorizontalLayout {
                        Rectangle { horizontal-stretch: 1; }
                        TextButton {
                            text: "Cancel";
                            enabled: !MainAdapter.core-busy;
                            clicked => {
                                MainAdapter.cancel-import();
                            }
                        }
                        FilledButton {
                            width: 120px;
                            text: MainAdapter.core-busy ? "Importing…" : "Import";
                            enabled: !MainAdapter.core-busy;
                            clicked => {
                                MainAdapter.import-profile(
                                    MainAdapter.import-name,
                                    MainAdapter.import-url
                                );
                            }
                        }
                    }
                }
            }

            Rectangle {
                width: 100%;
                min-height: 100px;
                border-radius: 18px;
                background: MaterialPalette.primary_container;
                border-width: 1px;
                border-color: MaterialPalette.primary;

                VerticalLayout {
                    spacing: 12px;
                    padding: 20px;
                    width: 100%;

                    HorizontalLayout {
                        spacing: 16px;

                        Rectangle {
                            width: 48px;
                            height: 48px;
                            border-radius: 16px;
                            background: MaterialPalette.primary;
                            Icon {
                                source: @image-url("../icons/emblem-default.svg");
                                width: 28px;
                                height: 28px;
                                x: (parent.width - self.width) / 2;
                                y: (parent.height - self.height) / 2;
                                colorize: MaterialPalette.on_primary;
                            }
                        }

                        VerticalLayout {
                            spacing: 4px;
                            MaterialText {
                                text: "Active Profile";
                                style: Typography.label_medium;
                                color: MaterialPalette.on_primary_container;
                            }
                            MaterialText {
                                text: MainAdapter.active-profile == "" ? "None" : MainAdapter.active-profile;
                                style: Typography.title_large;
                                color: MaterialPalette.on_primary_container;
                            }
                            MaterialText {
                                text: MainAdapter.active-profile == ""
                                    ? "Choose a profile below to continue"
                                    : "Currently used by Mihomo";
                                style: Typography.label_small;
                                color: MaterialPalette.on_primary_container;
                                opacity: 0.72;
                            }
                        }
                    }
                }
            }

            if !MainAdapter.profiles-loaded || MainAdapter.profiles.length == 0: Rectangle {
                width: 100%;
                min-height: 240px;
                border-radius: 20px;
                background: MaterialPalette.surface_container_low;
                border-width: 1px;
                border-color: MaterialPalette.outline_variant;

                VerticalLayout {
                    alignment: center;
                    spacing: 12px;
                    padding: 32px;
                    width: 100%;

                    HorizontalLayout {
                        alignment: center;
                        Rectangle {
                            width: 72px;
                            height: 72px;
                            border-radius: 24px;
                            background: MaterialPalette.surface_container_highest;
                            Icon {
                                source: @image-url("../icons/folder.svg");
                                width: 36px;
                                height: 36px;
                                x: (parent.width - self.width) / 2;
                                y: (parent.height - self.height) / 2;
                                colorize: MaterialPalette.primary;
                            }
                        }
                    }
                    MaterialText {
                        text: "No profiles found";
                        style: Typography.title_large;
                        horizontal-alignment: center;
                    }
                    MaterialText {
                        text: "Import a subscription or add YAML files to the config directory.";
                        style: Typography.body_medium;
                        horizontal-alignment: center;
                        color: MaterialPalette.on_surface_variant;
                    }
                }
            }

            if MainAdapter.profiles-loaded && MainAdapter.profiles.length > 0: ElevatedCard {
                width: 100%;
                VerticalLayout {
                    spacing: 0px;
                    padding: 20px;
                    width: 100%;

                    HorizontalLayout {
                        spacing: 8px;
                        width: 100%;
                        Icon {
                            source: @image-url("../icons/folder-open.svg");
                            width: 20px;
                            height: 20px;
                        }
                        MaterialText {
                            text: "Available Profiles";
                            style: Typography.title_small;
                        }
                    }

                    HorizontalDivider { width: 100%; }

                    for profile[idx] in MainAdapter.profiles: VerticalLayout {
                        width: 100%;
                        HorizontalLayout {
                            spacing: 12px;
                            padding-left: 4px;
                            padding-right: 4px;
                            padding-top: 12px;
                            padding-bottom: 12px;
                            alignment: center;
                            width: 100%;

                            Icon {
                                source: profile.name == MainAdapter.active-profile ? @image-url("../icons/emblem-default.svg") : @image-url("../icons/folder.svg");
                                width: 20px;
                                height: 20px;
                            }

                            MaterialText {
                                text: profile.name;
                                style: Typography.body_medium;
                            }

                            Rectangle { horizontal-stretch: 1; }

                            if profile.is-subscription: TextButton {
                                text: "Update";
                                enabled: !MainAdapter.core-busy;
                                clicked => {
                                    MainAdapter.update-profile(profile.name);
                                }
                            }

                            if MainAdapter.active-profile == profile.name: MaterialText {
                                text: "Selected";
                                style: Typography.label_medium;
                                opacity: 0.6;
                            }

                            if MainAdapter.active-profile != profile.name: TextButton {
                                text: "Select";
                                enabled: !MainAdapter.core-busy;
                                clicked => {
                                    MainAdapter.select-profile(profile.name);
                                }
                            }
                        }
                    }
                }
            }
        }
    }
}
